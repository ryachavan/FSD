services:
  - type: web
    name: student-feedback
    env: node
    plan: free
    buildCommand: npm install --omit=optional && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
